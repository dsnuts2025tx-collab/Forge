const json = (data, status = 200, extra = {}) => Response.json(data, { status, headers: { "Cache-Control": "no-store", ...extra } });
const corsHeaders = { "Access-Control-Allow-Origin": "*", "Access-Control-Allow-Methods": "GET, POST, OPTIONS", "Access-Control-Allow-Headers": "Content-Type, Authorization" };
const enc = new TextEncoder();
const b64u = bytes => btoa(String.fromCharCode(...new Uint8Array(bytes))).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/g, "");
const fromB64u = value => Uint8Array.from(atob(value.replace(/-/g, "+").replace(/_/g, "/") + "==="), c => c.charCodeAt(0));
async function hmac(secret, value) { const key = await crypto.subtle.importKey("raw", enc.encode(secret), { name: "HMAC", hash: "SHA-256" }, false, ["sign", "verify"]); return new Uint8Array(await crypto.subtle.sign("HMAC", key, enc.encode(value))); }
async function signSession(payload, secret) { const body = b64u(enc.encode(JSON.stringify(payload))); return `${body}.${b64u(await hmac(secret, body))}`; }
async function verifySession(token, secret) { try { const [body, sig] = token.split("."); if (!body || !sig) return null; const key = await crypto.subtle.importKey("raw", enc.encode(secret), { name: "HMAC", hash: "SHA-256" }, false, ["verify"]); if (!(await crypto.subtle.verify("HMAC", key, fromB64u(sig), enc.encode(body)))) return null; const payload = JSON.parse(new TextDecoder().decode(fromB64u(body))); return payload.exp && payload.exp >= Math.floor(Date.now()/1000) ? payload : null; } catch { return null; } }
async function stripeRequest(env, path, params = {}) { if (!env.STRIPE_SECRET_KEY) throw new Error("Stripe is not configured"); const body = new URLSearchParams(); for (const [k,v] of Object.entries(params)) body.set(k,String(v)); const r = await fetch(`https://api.stripe.com/v1/${path}`, { method:"POST", headers:{Authorization:`Bearer ${env.STRIPE_SECRET_KEY}`,"Content-Type":"application/x-www-form-urlencoded"}, body }); const text=await r.text(); let data; try{data=JSON.parse(text)}catch{data={raw:text}} if(!r.ok) throw new Error(data?.error?.message||`Stripe request failed (${r.status})`); return data; }
async function verifyStripeSignature(payload, header, secret) { if(!header||!secret)return false; const parts=Object.fromEntries(header.split(",").map(p=>p.split("=",2))); const timestamp=Number(parts.t), signature=parts.v1; if(!timestamp||!signature||Math.abs(Date.now()/1000-timestamp)>300)return false; const expected=b64u(await hmac(secret,`${timestamp}.${payload}`)); return expected===signature; }
export class ForgeState { constructor(state){this.state=state;this.storage=state.storage} async fetch(request){const url=new URL(request.url);if(request.method==="OPTIONS")return new Response(null,{status:204,headers:corsHeaders});if(request.method==="GET"&&url.pathname==="/state"){const snapshot=(await this.storage.get("snapshot"))||{version:2,projects:[],entitlements:{},updatedAt:null};return json({ok:true,snapshot},200,corsHeaders)}if(request.method==="POST"&&url.pathname==="/state"){const body=await request.json().catch(()=>null);if(!body||typeof body!=="object")return json({ok:false,error:"Invalid state payload"},400,corsHeaders);const previous=(await this.storage.get("snapshot"))||{};const snapshot={version:2,projects:Array.isArray(body.projects)?body.projects:(previous.projects||[]),entitlements:body.entitlements&&typeof body.entitlements==="object"?body.entitlements:(previous.entitlements||{}),updatedAt:new Date().toISOString()};await this.storage.put("snapshot",snapshot);return json({ok:true,snapshot},200,corsHeaders)}if(request.method==="POST"&&url.pathname==="/entitlement"){const body=await request.json().catch(()=>null);if(!body?.customerId)return json({ok:false,error:"Missing customerId"},400,corsHeaders);const snapshot=(await this.storage.get("snapshot"))||{version:2,projects:[],entitlements:{}};snapshot.entitlements=snapshot.entitlements||{};snapshot.entitlements[body.customerId]={active:!!body.active,status:body.status||"unknown",subscriptionId:body.subscriptionId||null,updatedAt:new Date().toISOString()};snapshot.updatedAt=new Date().toISOString();await this.storage.put("snapshot",snapshot);return json({ok:true},200,corsHeaders)}return json({ok:false,error:"Not found"},404,corsHeaders)}}
export default { async fetch(request,env){const url=new URL(request.url);if(request.method==="OPTIONS")return new Response(null,{status:204,headers:corsHeaders});if(url.pathname==="/health")return json({ok:true,service:"forge",engine:"6.0.0-PLATINUM",mode:"production",architecture:"native-durable-state"});if(url.pathname==="/api/status"){const stripeConfigured=!!env.STRIPE_SECRET_KEY,billingConfigured=stripeConfigured&&!!env.FORGE_PRICE_ID,sessionConfigured=!!env.FORGE_SESSION_SECRET;return json({ok:true,forge:"Forge Engine 6.0.0-PLATINUM",production:true,build:true,preview:true,projects:true,export:true,deployment:false,durableState:!!env.FORGE_STATE,billingConfigured,sessionConfigured,aiConfigured:!!env.OPENROUTER_API_KEY,note:billingConfigured&&sessionConfigured&&env.OPENROUTER_API_KEY?"Production gates configured":"Production configuration still required"})}if(url.pathname==="/api/billing/config"&&request.method==="GET")return json({ok:true,checkoutReady:!!env.STRIPE_SECRET_KEY&&!!env.FORGE_PRICE_ID&&!!env.FORGE_SESSION_SECRET});if(url.pathname==="/api/billing/checkout"&&request.method==="POST"){if(!env.STRIPE_SECRET_KEY||!env.FORGE_PRICE_ID)return json({ok:false,error:"Forge billing is not configured"},503);const body=await request.json().catch(()=>({}));try{const session=await stripeRequest(env,"checkout/sessions",{mode:"subscription","line_items[0][price]":env.FORGE_PRICE_ID,"line_items[0][quantity]":1,success_url:`${url.origin}/billing.html?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${url.origin}/billing.html?cancelled=1`,customer_email:body.email||""});return json({ok:true,url:session.url})}catch(error){return json({ok:false,error:error.message},502)}}if(url.pathname==="/api/billing/session"&&request.method==="GET"){if(!env.STRIPE_SECRET_KEY||!env.FORGE_SESSION_SECRET)return json({ok:false,error:"Billing session is not configured"},503);const sessionId=url.searchParams.get("session_id");if(!sessionId)return json({ok:false,error:"Missing session_id"},400);try{const r=await fetch(`https://api.stripe.com/v1/checkout/sessions/${encodeURIComponent(sessionId)}`,{headers:{Authorization:`Bearer ${env.STRIPE_SECRET_KEY}`}}),data=await r.json();if(!r.ok||!data.customer)return json({ok:false,error:"Checkout session not complete"},400);const token=await signSession({customerId:data.customer,exp:Math.floor(Date.now()/1000)+2592000},env.FORGE_SESSION_SECRET);return json({ok:true,token,customerId:data.customer})}catch(error){return json({ok:false,error:error.message},502)}}if(url.pathname==="/api/entitlement"&&request.method==="GET"){if(!env.FORGE_SESSION_SECRET||!env.FORGE_STATE)return json({ok:false,error:"Entitlement service is not configured"},503);const auth=request.headers.get("Authorization")||"",token=auth.startsWith("Bearer ")?auth.slice(7):"",session=await verifySession(token,env.FORGE_SESSION_SECRET);if(!session)return json({ok:false,error:"Unauthorized"},401);const id=env.FORGE_STATE.idFromName(session.customerId),state=await env.FORGE_STATE.get(id).fetch(new Request(new URL("/state",url),{method:"GET"})),data=await state.json();return json({ok:true,entitlement:data.snapshot?.entitlements?.[session.customerId]||{active:false,status:"pending"}})}if(url.pathname==="/api/stripe/webhook"&&request.method==="POST"){if(!env.STRIPE_WEBHOOK_SECRET||!env.FORGE_STATE)return json({ok:false,error:"Webhook service is not configured"},503);const raw=await request.text();if(!(await verifyStripeSignature(raw,request.headers.get("Stripe-Signature"),env.STRIPE_WEBHOOK_SECRET)))return json({ok:false,error:"Invalid Stripe signature"},400);let event;try{event=JSON.parse(raw)}catch{return json({ok:false,error:"Invalid webhook JSON"},400)}const object=event.data?.object||{},customerId=object.customer;if(customerId&&["checkout.session.completed","customer.subscription.created","customer.subscription.updated","customer.subscription.deleted"].includes(event.type)){const active=event.type==="checkout.session.completed"||object.status==="active"||object.status==="trialing",id=env.FORGE_STATE.idFromName(customerId);await env.FORGE_STATE.get(id).fetch(new Request(new URL("/entitlement",url),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({customerId,active,status:object.status||"active",subscriptionId:object.subscription||object.id})}))}return json({received:true})}if(url.pathname==="/api"&&request.method==="POST"){const body=await request.json().catch(()=>null),prompt=typeof body?.prompt==="string"?body.prompt.trim():"",provider=body?.provider||"openrouter";if(!prompt)return json({ok:false,error:"Missing prompt"},400,corsHeaders);if(provider!=="openrouter")return json({ok:false,error:"Unsupported provider"},400,corsHeaders);if(!env.OPENROUTER_API_KEY)return json({ok:false,error:"Forge AI is not configured yet. Set OPENROUTER_API_KEY as a Worker secret."},503,corsHeaders);try{const ai=await fetch("https://openrouter.ai/api/v1/chat/completions",{method:"POST",headers:{Authorization:`Bearer ${env.OPENROUTER_API_KEY}`,"Content-Type":"application/json","HTTP-Referer":url.origin,"X-Title":"Forge AI"},body:JSON.stringify({model:"deepseek/deepseek-r1:free",temperature:.7,max_tokens:2000,messages:[{role:"system",content:'You are Forge AI. Return ONLY valid JSON. {"projectName":"","description":"","files":[{"path":"","content":""}]}'},{role:"user",content:prompt}]})});const text=await ai.text();if(!ai.ok)return new Response(text,{status:ai.status,headers:{"Content-Type":"application/json",...corsHeaders}});let data;try{data=JSON.parse(text)}catch{return json({ok:false,error:"AI provider returned invalid JSON"},502,corsHeaders)}return json({ok:true,project:data},200,corsHeaders)}catch(error){return json({ok:false,error:`Forge AI request failed: ${error.message}`},502,corsHeaders)}}if(url.pathname.startsWith("/api/state/")){if(!env.FORGE_STATE)return json({ok:false,error:"Durable state binding is unavailable"},503);const key=url.pathname.slice("/api/state/".length)||"default",id=env.FORGE_STATE.idFromName(key),stub=env.FORGE_STATE.get(id),forwarded=new URL(request.url);forwarded.pathname=url.pathname.replace("/api/state/","/state");return stub.fetch(new Request(forwarded,request))}const asset=await env.ASSETS?.fetch(request);if(asset)return asset;return new Response("Forge Engine 6.0.0-PLATINUM",{status:404,headers:{"content-type":"text/plain; charset=utf-8"}})}};
